# 2026-03-02 开发日志

## 阶段 1：项目骨架初始化（Task 1）

- 已在隔离工作树 `fluxdeck-mvp` 执行。
- TDD 验证：
  - 红阶段：`cargo test -q` 在无 `Cargo.toml` 时失败。
  - 绿阶段：补齐最小骨架后 `cargo test -q` 通过。
- 新增内容：
  - Rust workspace 与 `fluxd` / `fluxctl` 启动入口
  - `apps/desktop` 最小占位结构
  - `README.md` 项目说明

## 下一阶段

- Task 2：SQLite schema 与 migration 基础设施。

## 阶段 2：SQLite migration 基础设施（Task 2）

- 新增 `crates/fluxd/migrations/001_init.sql`，完成核心表初始化：
  - `providers`
  - `provider_models`
  - `gateways`
  - `request_logs`
- 新增 `crates/fluxd/src/storage/migrate.rs`，实现 `run_migrations`。
- 新增集成测试 `crates/fluxd/tests/storage_migration_test.rs`，验证 migration 后核心表存在。
- 验证结果：
  - `cargo test -p fluxd migration_creates_core_tables -q` 通过。
  - `cargo test -p fluxd -q` 通过。

## 下一阶段

- Task 3：Provider 数据模型与 CRUD 服务层。

## 阶段 3：Provider 数据模型与 CRUD（Task 3）

- 新增领域模型：`crates/fluxd/src/domain/provider.rs`
- 新增仓储层：`crates/fluxd/src/repo/provider_repo.rs`
- 新增服务层：`crates/fluxd/src/service/provider_service.rs`
- 新增测试：`crates/fluxd/tests/provider_service_test.rs`
- 实现能力：
  - `create_provider`
  - `get_provider_by_id`
  - `list_providers`
- 验证结果：
  - `cargo test -p fluxd create_and_get_provider -q` 通过。
  - `cargo test -p fluxd -q` 通过。

## 下一阶段

- Task 4：Gateway 数据模型与多网关运行时管理。
