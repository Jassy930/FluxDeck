# 2026-03-04 开发日志

## 阶段 A：Anthropic 原生转发实现（Task 1-3）

### Task 1：网关协议图配置字段落地

- 新增迁移：`crates/fluxd/migrations/002_gateway_protocol_config.sql`
- `gateways` 新增字段：
  - `upstream_protocol`（默认 `provider_default`）
  - `protocol_config_json`（默认 `{}`）
- 同步更新：
  - `crates/fluxd/src/domain/gateway.rs`
  - `crates/fluxd/src/repo/gateway_repo.rs`
  - `crates/fluxd/src/http/admin_routes.rs`（最小编译修复）
  - `crates/fluxd/tests/storage_migration_test.rs`
  - `crates/fluxd/tests/admin_api_test.rs`
- 提交：`45f09b6`
- 后续修复：同步 Admin API 契约文档与断言强度，提交 `8c15e18`

### Task 2：Protocol IR 与 Registry 骨架

- 新增模块：
  - `crates/fluxd/src/protocol/mod.rs`
  - `crates/fluxd/src/protocol/ir.rs`
  - `crates/fluxd/src/protocol/error.rs`
  - `crates/fluxd/src/protocol/registry.rs`
- 在 `crates/fluxd/src/lib.rs` 导出 `protocol` 模块
- 新增测试：`crates/fluxd/tests/protocol_registry_test.rs`
- 默认注册：`anthropic -> openai`
- 提交：`6a61258`

### Task 3：Anthropic 请求解码器（非流式）

- 新增适配器与解码器：
  - `crates/fluxd/src/protocol/adapters/mod.rs`
  - `crates/fluxd/src/protocol/adapters/anthropic/mod.rs`
  - `crates/fluxd/src/protocol/adapters/anthropic/request_decoder.rs`
- 新增测试：`crates/fluxd/tests/anthropic_decoder_test.rs`
- 关键能力：
  - `decode_anthropic_request` 解析 `model/system/messages/tools`
  - unknown 字段进入 `extensions`
  - 明确 `DecodeError` 分层（缺失字段/类型错误/结构错误）
  - 对坏输入不再静默吞掉
- 提交：
  - 初版：`1ead7ad`
  - 规格修复：`b43f79e`
  - 质量修复：`3132f1e`

## 阶段验证

- `cargo test -p fluxd --test anthropic_decoder_test -q`：`6 passed`
- `cargo test -p fluxd -q`：通过
- `git status --short`：工作区干净

## 下一阶段

- Task 4：实现 OpenAI 请求编码器与上游调用抽象

### Task 4：OpenAI 请求编码器与上游调用抽象

- 新增文件：
  - `crates/fluxd/src/protocol/adapters/openai/mod.rs`
  - `crates/fluxd/src/protocol/adapters/openai/request_encoder.rs`
  - `crates/fluxd/tests/openai_encoder_test.rs`
- 变更文件：
  - `crates/fluxd/src/protocol/adapters/mod.rs`
  - `crates/fluxd/src/upstream/openai_client.rs`
- 核心能力：
  - `encode_openai_chat_request(ir)` 完成 `model/messages/tools` 编码
  - `system_parts` 映射为 `role=system` 消息
  - Anthropic tool 定义归一为 OpenAI `function` tools
  - `openai_client` 增加 `chat_completions_from_ir` 便捷入口
- 质量补丁：
  - 补齐 `tools` 异常路径测试（缺 name、缺 input_schema、非 object）
- 相关提交：
  - `a55d3e0`（Task4 主提交）
  - `37780da`（system/tools 语义修复）
  - `7e0d8ed`（tools 异常路径测试补齐）
- 验证结果：
  - `cargo test -p fluxd --test openai_encoder_test -q`：`7 passed`
  - `cargo test -p fluxd -q`：通过
  - `git status --short`：干净

### Task 5：Anthropic 非流式 `/v1/messages` 路由

- 新增路由：`crates/fluxd/src/http/anthropic_routes.rs`
- 导出路由模块：`crates/fluxd/src/http/mod.rs`
- 新增 e2e 测试：`crates/fluxd/tests/anthropic_forwarding_test.rs`
- 主链路：
  - Anthropic 请求解码到 IR
  - IR 编码为 OpenAI chat payload 并请求上游
  - OpenAI 响应映射回 Anthropic message
- 质量修复：
  - 修复 `tool_calls -> tool_use` 映射一致性
  - 本地编码失败改为 `400 + invalid_request_error`
  - 强制 `tool_use.input` 始终为 object（对象原样/非对象包装）
- 任务相关提交：
  - `5629ae8`（Task5 主提交）
  - `0a343b4`（语义与错误分类修复）
  - `94ee495`（tool_use.input 对象化修复）
- 补充设计记录：`docs/plans/2026-03-04-task5-review-fixes.md`
- 验证结果：
  - `cargo test -p fluxd --test anthropic_forwarding_test -q`：`4 passed`
  - `cargo test -p fluxd -q`：通过
  - `git status --short`：干净

### Task 6：Anthropic 流式 SSE 事件映射（Anthropic <-> OpenAI）

- 新增统一流式事件模型：
  - `crates/fluxd/src/protocol/stream/mod.rs`
- 新增适配器：
  - `crates/fluxd/src/protocol/adapters/openai/stream_decoder.rs`
  - `crates/fluxd/src/protocol/adapters/anthropic/stream_encoder.rs`
- 路由改造：
  - `crates/fluxd/src/http/anthropic_routes.rs` 增加 `stream=true` 分支
  - 对上游 `chat.completions` 以流式调用，解码 OpenAI SSE 增量后编码为 Anthropic SSE 事件
  - 非流式路径保持原有映射行为
- 新增测试：
  - `crates/fluxd/tests/anthropic_streaming_test.rs`
  - 断言 `text/event-stream` 与 `message_start/content_block_delta/message_stop` 事件链路
- 同步导出与客户端补充：
  - `crates/fluxd/src/protocol/mod.rs`
  - `crates/fluxd/src/protocol/adapters/anthropic/mod.rs`
  - `crates/fluxd/src/protocol/adapters/openai/mod.rs`
  - `crates/fluxd/src/upstream/openai_client.rs`（新增流式请求方法）
- 验证结果：
  - `cargo test -p fluxd --test anthropic_streaming_test -q`：`1 passed`
  - `cargo test -p fluxd -q`：通过
- 质量补丁（Task6 Review Fix）：
  - 提交 `891732e`：改为真正增量流式输出（边读上游 SSE 边向下游输出）
  - 新增增量链路组件：`OpenAiSseDecoder` / `AnthropicSseEncoder`
  - 新增与增强测试：
    - `maps_openai_sse_to_anthropic_sse_events`
    - `streams_anthropic_sse_incrementally_before_upstream_completes`
    - `extracts_error_message_from_sse_wrapped_error_payload`
  - 补充记录文档：`docs/plans/2026-03-04-task6-quality-fixes.md`
- 最新验证结果（Task6 收尾）：
  - `cargo test -p fluxd --test anthropic_streaming_test -q`：`3 passed`
  - `cargo test -p fluxd -q`：通过
  - `git status --short`：干净

### Task 7：实现 `/v1/messages/count_tokens` 与降级策略

- 路由扩展：
  - `crates/fluxd/src/http/anthropic_routes.rs` 新增 `POST /v1/messages/count_tokens`
  - 能力策略：
    - 上游 `200` 且返回 `input_tokens`：直返并标记 `estimated=false`
    - 上游 `404/405/501`：降级本地估算并标记 `estimated=true`
    - 其他上游错误：透传状态并映射 Anthropic 错误结构
- 协议与客户端：
  - `crates/fluxd/src/protocol/token_count.rs` 新增 `count_tokens/estimate_tokens`
  - `crates/fluxd/src/protocol/mod.rs` 导出 `token_count`
  - `crates/fluxd/src/upstream/openai_client.rs` 新增 `anthropic_messages_count_tokens`
- 新增测试：
  - `crates/fluxd/tests/anthropic_count_tokens_test.rs`
  - 覆盖“上游原生计数”与“上游不支持时本地估算”两条路径
- 提交：`95fe9d2`
- 验证结果：
  - `cargo test -p fluxd --test anthropic_count_tokens_test -q`：`2 passed`
  - `cargo test -p fluxd -q`：通过

### Task 8：改造网关运行时按协议图选择路由

- 运行时路由选择：
  - `crates/fluxd/src/runtime/gateway_manager.rs`
  - `start_gateway_inner` 按 `inbound_protocol` 构建路由：
    - `openai` -> `build_openai_router`
    - `anthropic` -> `build_anthropic_router`
    - 其他值 -> 启动失败并返回 `unsupported inbound protocol` 错误
- 新增测试：
  - `crates/fluxd/tests/gateway_manager_test.rs`
  - `routes_gateway_runtime_by_inbound_protocol`
    - OpenAI 网关访问 `/v1/messages` 返回 `404`
    - Anthropic 网关访问 `/v1/messages` 不应为 `404`（命中 Anthropics 路由）
- 测试先行记录：
  - 先运行 `cargo test -p fluxd --test gateway_manager_test -q`，新增用例失败（Anthropic 仍返回 404）
  - 完成实现后同命令转绿：`2 passed`
- 提交：`bc5911c`
- 回归验证：
  - `cargo test -p fluxd -q`：通过
